server:
  port: 4000

spring:
  application:
    name: EkartGateway
  cloud:
    consul:
      host: localhost
      # discovery:
      #   health-check-url: http://localhost:${server.port}${server.servlet.context-path}/actuator
